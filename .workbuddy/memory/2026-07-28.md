# 2026-07-28

## System Prompt Compatibility 实现

### 完成内容
- **Task 1 (已就绪)**: `_normalize_developer_messages_to_system()` - developer → system 角色归一化
- **Task 2 (本次完成)**: 添加 `DEFAULT_SYSTEM_PROMPT_COMPATIBILITY_REPLACEMENTS` 三条默认替换规则，修改 `_load_system_prompt_sensitive_replacements()` 使空配置时使用默认规则、有效 JSON 配置时覆盖默认规则
- **Task 3 (本次完成)**: 更新 `docs/codex-claude-message-compatibility-notes.md` 反映实际实现

### 关键实现细节
- 默认规则: "Claude Code CLI" → "Claude Agent SDK", "PRs" → "pull requests", issues URL → 项目主页
- `system_prompt_sensitive_replacements` 默认值从 `"[]"` 改为 `""`（空字符串触发默认规则）
- 替换仅影响 system 角色消息（含归一化后的 developer）
- 所有 43 个测试通过，1 个 skip

### 提交
- `e9551a0` feat(proxy): normalize developer messages to system
- `fae12bb` docs: document system prompt compatibility behavior
