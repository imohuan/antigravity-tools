# 2026-07-07

## Added daily quota sync scheduler
- 新增 `web/scheduler.py`：APScheduler 后台定时任务，每天 08:00 (Asia/Shanghai) 调用 `query_all_quotas()` 刷新所有账户额度
- `requirements.txt` 添加 `apscheduler==3.11.0`
- `web/server.py` 的 `main()` 中 `init_db()` 之后启动 scheduler
- 任务失败会写 error 日志，不影响服务正常运行
