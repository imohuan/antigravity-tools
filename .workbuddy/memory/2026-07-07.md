## 2026-07-07

- 前端批量签到改为并发调用单个 `/api/checkin/{uid}` 接口。
- 新增 `runWithConcurrency()` 通用并发池，使用 `settings.checkinConcurrency` 控制并发数（1-50，默认 5）。
- 设置页面新增"签到并发数"输入框，保存时通过 `/api/settings` 一并提交。
- 未修改后台，仅改动 `web/static/index.html`。
